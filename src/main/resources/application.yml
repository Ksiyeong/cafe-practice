spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create # ??? ?? ??
    show-sql: true # SQL ?? ??
    properties:
      hibernate:
        format_sql: false # SQL pretty print